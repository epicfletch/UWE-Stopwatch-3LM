#include "AlarmSetFSM.h"
void alarmSetFSM()
{
    uint8_t alarmState = 0;   // variable for the alarm state
    while(1)
    {
        switch (alarmState)
        {
            default:
                break;
        }
    }
}